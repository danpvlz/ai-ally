---
import { twMerge } from 'tailwind-merge';
interface Props {
    className?: string;
}
const { className } = Astro.props
---

<h2 class={twMerge(`text-52 leading-none font-medium`,className)}><slot /></h2>